# 鳴潮抽卡期望值計算機 — 抽卡邏輯說明

> 本文件依據 `dist/main.js` 的實作整理，說明目前計算機內部的抽卡模擬邏輯。

## 整體架構

- `runSim(params, 100000)`（main.js:505）：跑 10 萬次 `simulate()`，加總平均，並排序算出 P10~P90 百分位。
- `simulate(p)`（main.js:248）：模擬完成所有目標所需的完整流程。
- 目標可以是**角色（character）**或**武器（weapon）**，依序處理，**共享珊瑚與常駐角色鏈數**，但**角色池與武器池的保底各自獨立**。

---

## 1. 兩套獨立的保底狀態

```js
// 角色池保底
let pity5, pity4, g5up, g4up;
// 武器池保底（與角色池獨立）
let wPity5, wPity4, wg4up;
```

- `pityN` ＝距離上次出 N 星累積的失敗抽數。
- `g5up` / `g4up` ＝角色池的 5 星 / 4 星「大保底（歪了下次必 UP）」旗標。
- 武器池只有 `wg4up`（4 星大保底），**沒有 5 星大保底**，因為武器池 5 星 100% 就是 UP。

---

## 2. 機率模型（兩池共用同一套公式）

### 5 星機率：軟保底遞增 `getRate5(p5)`（main.js:285）

`p5` 傳入角色池的 `pity5` 或武器池的 `wPity5`：

| 抽數區間 (p5) | 公式 | 機率 |
|---|---|---|
| ≤ 64（第 1~65 抽） | `0.008` | 0.8% |
| ≤ 69（66~70） | `0.008 + (p5-64)*0.04` | 每抽 +4% |
| ≤ 74（71~75） | `0.008 + 5*0.04 + (p5-69)*0.08` | 每抽 +8% |
| ≤ 77（76~78） | `0.008 + 5*0.04 + 5*0.08 + (p5-74)*0.10` | 每抽 +10% |
| ≥ 78（第 79 抽起） | `1.0` | 100% 硬保底 |

### 4 星機率與保底

```js
const delta = rate5 - 0.008;                  // 5星軟保底多出來的量
const rate4 = Math.max(0, 0.06 - delta / 2);  // 4星等比下降一半
```

- 基礎 4 星 = 6%；5 星機率上升時，4 星被擠壓 `delta/2`。
- **4 星保底**：`pity4 >= 9`（連 9 抽沒出 4 星↑）時，這抽只會是 5 星或 4 星，不會是 3 星。

### 稀有度判定（`r1` 是這一抽的隨機骰子，0~1）

```js
const r1 = Math.random();
if (pity4 >= 9) {
  rarity = r1 < rate5 ? 5 : 4;
} else {
  if      (r1 < rate5)          rarity = 5;
  else if (r1 < rate5 + rate4)  rarity = 4;
  else                          rarity = 3;
}
```

把 0~1 切成數段，`r1` 落在哪段就抽到對應稀有度。判定後更新計數：5 星→兩計數歸零；4 星→`pity5++, pity4=0`；3 星→兩者都 `++`。

---

## 3. 角色池出貨邏輯 `pull()`（main.js:390）

**5 星時**：
- `g5up` 為真 → 必 UP（用掉旗標）；否則 50% UP，歪了設 `g5up`。
- 抽到 UP → `upCopies++`，珊瑚用 `coralFor5Star`。
- 抽到常駐 → 隨機 5 名之一 `std5[ci]++`，珊瑚 = 重複獎勵 + 歪卡 bonus（一般 30 / 連動 25）。

**4 星時**：
- `g4up` 同理控制大保底，50% 為 UP。
- UP → 從你選的 3 名 4 星 UP 隨機 1 名。
- 非 UP → `Math.floor(random*29)`：0~8 是 9 名常駐 4 星角色，9~28 是 20 把 4 星武器（武器給固定珊瑚 3 / 連動 2）。

---

## 4. 武器池出貨邏輯 `simulateWeaponTarget()`（main.js:306）

武器目標單獨處理：

- **完成條件** `isDone()`：`upCopies - 1 >= targetRefine`，即目前精煉階級達到目標（武器最高 +5 精煉）。
- **5 星武器 100% 為 UP**（不像角色池有 50% 歪卡機制）：`upCopies++`，珊瑚一般 15 / 連動 12。
- **4 星**：`wg4up` 控制大保底，50% UP。
  - UP 武器 → 固定珊瑚 3 / 連動 2。
  - 非 UP → `Math.floor(random*32)`：0~11 是 12 名常駐 4 星角色，12~31 是 20 把 4 星武器。
- 武器池抽數也會累進聯動里程碑（`milestoneTotalPulls++`），但**里程碑的捕夢波紋和回音頻段只能用在角色池**。

---

## 5. 完成判定與珊瑚／鏈數（角色池）

### `isDone()`（main.js:386）

```js
return upCopies >= 1 && (upCopies - 1 + milestoneEchoes + shopEchoes) >= targetChains;
```

要先有本體，再看「多餘本體 + 里程碑免費回音 + 珊瑚兌換回音」≥ 目標鏈數。後兩者合計上限 2。

### 珊瑚遞增規則（main.js:241）

```js
coralFor5Star:     copies < 7 ? 15 : 40   (連動 12 / 30)
coralFor4StarChar: copies < 7 ? 3  : 8    (連動 2 / 5)
```

---

## 6. 自動花費珊瑚（已簡化）

> 舊版的「自動用珊瑚換波紋抽（autoPull）」和「優先順序選項」已被移除。現在只剩自動兌換回音頻段。

```js
function applyActions() {
  if (isDone()) return;
  buyEchoes();
}
```

`buyEchoes()`（main.js:473）：
1. 先套用里程碑免費回音（`applyMilestoneEchoes`，免費、優先）。
2. 若開啟 `autoEcho` 且有本體且名額未滿 2，珊瑚每 360 換一個回音頻段。

珊瑚是否轉成抽數，改在**結果頁**用「剩餘珊瑚 ÷ 8」估算，不再於模擬迴圈內換波紋抽。

---

## 7. 電馭叛客連動里程碑（改為「先存後用」）

`CYBERPUNK_MILESTONES`（main.js:4）：第 20/40/80…抽達標送捕夢波紋或回音頻段。新版分成兩步：

- `bankMilestones()`（main.js:295）：把達標的獎勵**存進待用池**（`pendingBonusPulls`、`pendingMilestoneEchoes`），不立即抽。角色池與武器池的抽數都會累進里程碑計數，所以兩池都呼叫它。
- `processMilestones()`（main.js:452）：**只在角色目標中**把存起來的捕夢波紋拿來免費抽（`pull()`），免費抽本身可能再觸發新里程碑，故迴圈處理。
- 回音頻段（`pendingMilestoneEchoes`）也只能套用在角色 UP 上。

> 也就是：武器池抽卡會「累積」里程碑進度，但實際兌現的免費抽 / 回音頻段都流向角色池。

---

## 8. 主迴圈與結果輸出

### 主迴圈（main.js:372）

```js
for (const target of p.targets) {
  if (target.type === 'weapon') { ...simulateWeaponTarget...; continue; }
  // 角色目標：
  applyActions();
  while (!isDone() && pullsThisTarget < 30000) {
    pull(); pullsThisTarget++;
    processMilestones();
    applyActions();
  }
}
```

分別累計 `charPulls`（角色池，吃浮金波紋）與 `weaponPulls`（武器池，吃鑄潮波紋）。

### 結果頁（main.js:704）顯示

- 預期總抽數、各目標抽數、P10~P90 分布。
- 珊瑚統計：初始 / 過程獲取 / 兌換回音用量 / 剩餘。
- **抽卡資源統計**（新版分角色池與武器池兩條線）：
  - 角色池抽數 → 扣里程碑免費抽 → 扣初始浮金波紋。
  - 武器池抽數 → 扣初始鑄潮波紋。
  - 兩池合計 = 「達成目的預計還須抽數」（×160 星聲）。
  - 再提供**兩欄對照**：不使用珊瑚兌換 vs. 使用剩餘珊瑚兌換抽數（÷8），各自換算還需星聲與真錢（依匯率）。

---

## 與舊版的主要差異總結

| 項目 | 舊版 | 新版 |
|---|---|---|
| 武器池 | 無 | 獨立保底、5 星 100% UP、精煉 +5 |
| 目標類型 | 只有角色鏈 | 角色（鏈）／武器（精煉）可選 |
| autoPull 自動換波紋抽 | 有，且有優先順序 | 移除，改在結果頁用珊瑚 ÷ 8 估算 |
| 里程碑獎勵 | 立即抽取 | 先存待用池，只流向角色池 |
| 結果頁 | 單一星聲估算 | 角色 / 武器雙線 + 用珊瑚 / 不用珊瑚雙欄 |
